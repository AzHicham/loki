ARG NAVITIA_TAG

FROM navitia/jormungandr:${NAVITIA_TAG}

ENV JORMUNGANDR_INSTANCES_DIR=/data/jormun_conf/
ENV JORMUNGANDR_DISABLE_DATABASE=True

VOLUME /data

CMD ["uwsgi", "--master", "--lazy-apps", "--mount", "/=jormungandr:app", "--http", "0.0.0.0:80"]
